<%= form_with(model: product) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
        <% product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :variant %>
    <%= form.text_field :variant %>
  </div>

  <div class="field">
    <%= form.label :recommended_price %>
    <%= form.text_field :recommended_price %>
  </div>

  <div class="field">
    <%= form.label :product_types_id %>
    <%= form.text_field :product_types_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
